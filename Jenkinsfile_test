node("master"){

//    println env_name

echo "Hello, ${name}."

                         _name_=name.split(",")[0]
                         branch=name.split(",")[1]
                         service=_name_.toLowerCase()
                         imag="${service}:v0.1."+env.BUILD_ID
                         println service
                         println branch
                         println imag
                         
                         
      stage('Clean up Workspace')      {
          deleteDir()
      }
      stage ('checkout')      {
          checkout scm
          sh "pwd;ls"
      }

      stage("Check"){
           try{
               sh "echo bob"
               }catch (Exception e) {
                       sh "echo Not exist"
                     }          
       }
      
      
}
